#!/usr/bin/env bash
# bin/release <build-dir>

BP_DIR=$(cd $(dirname $0)/..; pwd) # absolute path
BUILD_DIR=$1

if [ ! -f $BUILD_DIR/Procfile ] ; then

cat << EOF
---
default_process_types:
  web: \$MULE_HOME/bin/mule -M-DPORT=\$PORT -M-Dmule.agent.enabled=true
EOF

fi
